import { OnModuleInit } from '@nestjs/common';
import { Absence, Assignment, AuditLogEntry, Day, DailyTimetableEntry, ExamSession, ExamTimetableEntry, Grade, GradeSubjectConfig, Holiday, LessonPlan, MasterTimetableEntry, NotificationItem, PendingTimetableChange, ReliefAssignment, RoomBooking, Role, RolePermissions, Room, SchoolClass, SchoolConfig, StudentGroup, Subject, SwapRequest, SyllabusItem, Teacher, TeacherPreference, Term, TimeSlot, TimetableVersion, User } from '../common/types.js';
export declare class StoreService implements OnModuleInit {
    schoolConfig: SchoolConfig;
    rolePermissions: RolePermissions;
    timeSlots: TimeSlot[];
    grades: Grade[];
    classes: SchoolClass[];
    subjects: Subject[];
    gradeSubjects: GradeSubjectConfig[];
    rooms: Room[];
    teachers: Teacher[];
    users: User[];
    masterTimetable: MasterTimetableEntry[];
    timetableVersions: TimetableVersion[];
    dailyTimetable: DailyTimetableEntry[];
    absences: Absence[];
    reliefAssignments: ReliefAssignment[];
    auditLog: AuditLogEntry[];
    notifications: NotificationItem[];
    lessonPlans: LessonPlan[];
    terms: Term[];
    currentTermId: string;
    studentGroups: StudentGroup[];
    holidays: Holiday[];
    examSessions: ExamSession[];
    examTimetable: ExamTimetableEntry[];
    roomBookings: RoomBooking[];
    swapRequests: SwapRequest[];
    teacherPreferences: TeacherPreference[];
    syllabusItems: SyllabusItem[];
    assignments: Assignment[];
    pendingTimetableChanges: PendingTimetableChange[];
    onModuleInit(): void;
    id(): `${string}-${string}-${string}-${string}-${string}`;
    addAudit(action: string, actor: string, details: string): void;
    addNotification(message: string, category: string, toRole?: Role, toUserId?: string): void;
    slotsForDay(day: Day): TimeSlot[];
    private seed;
    private generateMasterTimetable;
    generateLessons(targetClassIds: string[], mode: 'FILL_GAPS' | 'REGENERATE'): {
        created: number;
        cleared: number;
        unresolved: string[];
    };
    private fillLessonsOnce;
    isHoliday(date: string): Holiday | undefined;
    materializeDailyTimetable(date: string, day: Day): void;
}
